# LoreWeaver

Small CLI tool for tracking story lore stuff (characters + events).
I made this as a side project and its pretty simple on purpose.

Run it:

python lore_manager.py

It saves data to lore_data.json in the same folder.

Submission note: upload only lore_manager.py.
